export function createMatchingWorkerUnsupportedNotice() {
  return "TRA050 MatchLab usa procesamiento por chunks compatible con HTML unico offline. El worker inline queda reservado para una fase posterior si se necesita aislar completamente el hilo principal.";
}
